# Scripts direkt aus Chat-GPT erstellt mit:

Ich möchte automatische das Passwort des Root setzen und einen Benutzer htl mit Passwort erstellen. 
Benutzernamen und Passwörter sollen in einer .env-Datei von Docker compose konfiguriert werden. 
Erstelle mir das neue Dockerfile und die zugehörige neue docker-compose.yml. 
Hier die bestehende docker-compose.yml und das aktuelle Dockerfile:

# Bilden und starten des Containers mit:
> docker compose up -d --build
